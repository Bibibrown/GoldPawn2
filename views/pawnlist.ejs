<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ข้อมูลการจำนำ</title>
    <link rel='stylesheet' href="/bootstrap/css/bootstrap.min.css" />
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mitr:wght@200;300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            font-family: "Mitr", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
    </style>
</head>

<body>
    <%- include('./partials/nav.ejs') %>

    <div class="container" style="margin-top: 7.7%;">
        <h2>ข้อมูลการจำนำของลูกค้า ID:</h2>
        <% if (pawn) { %>
    <table>
        <thead>
            <tr>
                <th>Pawn ID</th>
                <th>Gold ID</th>
                <th>Type Name</th>
                <th>Weight</th>
                <th>Principal</th>
                <th>Interest</th>
                <th>Interest per Month</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><%= pawn.pawnId %></td>
                <td>
                    <% if (Array.isArray(pawn.goldId)) { %>
                        <% pawn.goldId.forEach(goldId => { %>
                            <a href="/pawn/gold-payment-history/<%= goldId %>"><%= goldId %></a><br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.typeName.typeName %><br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.weight %> กรัม<br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.principal.toLocaleString() %> บาท<br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.interest %>%<br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.intperm %>บาท<br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
                <td>
                    <% if (Array.isArray(pawn.goldItems)) { %>
                        <% pawn.goldItems.forEach(gold => { %>
                            <%= gold.status %><br>
                        <% }); %>
                    <% } else { %>
                        N/A
                    <% } %>
                </td>
            </tr>
        </tbody>
    </table>
<% } else { %>
    <p>ไม่พบข้อมูลการจำนำ</p>
<% } %>
    </div>
</body>

</html>
